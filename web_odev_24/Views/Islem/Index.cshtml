@model IEnumerable<web_odev_24.Models.Islem>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.islem_ad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.islem_ucret)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.calisan)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.islem_ad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.islem_ucret)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.calisan.calisan_ad)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.islemID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.islemID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.islemID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
